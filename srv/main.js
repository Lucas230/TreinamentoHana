/*const cds = require('@sap/cds')
const { Books } = cds.entities('sap.capire.bookshop')

class MainService extends cds.ApplicationService {
    init() {
        this.after('UPDATE', 'DespesasSet', req => {
            
        })

        return super.init()
    }
}

module.exports = { MainService }*/